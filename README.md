## Collection of Message of the Day Scripts

Inspired by [yboetz/motd](https://github.com/yboetz/motd).

Script may need the following installed:

    apt install figlet bsdmainutils

Run `install-docker.sh` to copy scripts relevant to a docker host to `/etc/update-motd.d/`.
